<div class="row">
  <div class="col s12">
    <h3>All Trainers</h3>
  </div>
</div>

<div class="row">
  <div class="col s12">
    <% @trainers.each do |trainer| %>
      <%= link_to trainer.name, trainer_path(trainer) %><br></br>
    <% end %>
  </div>
</div>
